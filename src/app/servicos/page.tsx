import data from "../../content/services.json";export default function Page(){return(<div><h1 className="text-3xl font-semibold mb-6">Serviços</h1><div className="space-y-6">{data.items.map((s:any,i:number)=>(<div key={i} className="border rounded p-4"><h2 className="text-xl font-semibold">{s.title}</h2>{s.sla && <p className="mt-1 text-sm opacity-70">Prazo padrão: {s.sla}</p>}<ul className="list-disc pl-6 mt-3">{s.bullets.map((b:string,idx:number)=>(<li key={idx}>{b}</li>))}</ul></div>))}</div></div>)}